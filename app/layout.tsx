import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/Header'
import Footer from '@/components/Footer'
import CosmicBadge from '@/components/CosmicBadge'

const inter = Inter({ 
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-inter'
})

export const metadata: Metadata = {
  title: 'American Museum of Natural History',
  description: 'Discover the wonders of our natural world through exhibitions, collections, educational programs, and special events at the American Museum of Natural History.',
  keywords: ['museum', 'natural history', 'exhibitions', 'science', 'education', 'dinosaurs', 'planetarium'],
  openGraph: {
    title: 'American Museum of Natural History',
    description: 'Discover the wonders of our natural world through exhibitions, collections, educational programs, and special events.',
    type: 'website',
    locale: 'en_US',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const bucketSlug = process.env.COSMIC_BUCKET_SLUG as string

  return (
    <html lang="en" className={inter.variable}>
      <body className={`${inter.className} min-h-screen flex flex-col`}>
        <Header />
        <main className="flex-1">
          {children}
        </main>
        <Footer />
        <CosmicBadge bucketSlug={bucketSlug} />
      </body>
    </html>
  )
}